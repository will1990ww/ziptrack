/* config.js — business config (progressive enhancement only).
   Contact links are hard-coded in HTML, so the site works without JS.
   Turn on Google Analytics: set gaId to your GA4 ID, e.g. "G-XXXXXXXXXX". */
window.SITE_CONFIG = { brand: "Balcony Master", whatsapp: "6583417888", gaId: "" };
